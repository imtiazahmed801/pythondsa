# a list where nodes are linked together
# each node contains: data and a pointer
# they are linked together by each node pointing to where in the memory the next node is placed

# linked lists: consist of nodes and a linear data structure 
# nodes in linked list store links to other nodes but array elements do not need to store links to other elements  

# e.g data:17|next:--> data:28|next:-->

# operations: get_size, find(data), add(data), remove(data)

# arrays: exisiting data structure in a prog language we can use 